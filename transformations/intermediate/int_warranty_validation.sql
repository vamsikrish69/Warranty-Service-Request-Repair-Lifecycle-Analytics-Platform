CREATE OR REPLACE VIEW INTERMEDIATE.INT_WARRANTY_VALIDATION AS

SELECT

    l.*,

    w.WARRANTY_ID,
    w.WARRAN*Y_TYPE,
    w.ENTITLEMENT_LEVEL,
 *  w.WARRANTY_STATUS,
    w.REPAIR_*OVERAGE_TYPE,

    w.ONSITE_ELIGIB*E_FLAG,
*  *w.MAILIN_ELIGIBLE_FLAG

FROM INTER*EDIATE.INT_CONTACT_SERVICE_LIFECYC*E l

LEFT JOIN STAGING.STG_WARRANT*_ENTITLEMENTS w
    ON l.ASSET_ID * w.ASSET_ID;



-------version 2-----

CREATE OR REPLACE VIEW INTERMEDIATE.INT_WARRANTY_VALIDATION AS

SELECT

    l.*,

    w.WARRANTY_ID,
    w.WARRAN*Y_TYPE,
    w.ENTITLEMENT_LEVEL,
 *  w.WARRANTY_STATUS,
    w.REPAIR_*OVERAGE_TYPE,

    w.ONSITE_ELIGIB*E_FLAG,
    w.MAILIN_ELIGIBLE_FLAG*

    w.WARRANTY_START_DATE,
    w*WARRANTY_END_DATE,

    CASE
     *  WHEN w.WARRANTY_ID IS NOT NULL T*EN TRUE
        ELSE FALSE
    END*AS WARRANTY_FOUND_FLAG,

    CASE
*       WHEN w.WARRANTY_STATUS = 'A*TIVE' THEN TRUE
        ELSE FALSE*    END AS ACTIVE_WARRANTY_FLAG,

*   CASE
        WHEN w.WARRANTY_ST*TUS = 'EXPIRED' THEN TRUE
        *LSE FALSE
    END AS EXPIRED_WARRA*TY_FLAG,

    CASE
        WHEN w.*NSITE_ELIGIBLE_FLAG = TRUE THEN TR*E
        ELSE FALSE
    END AS ON*ITE_REPAIR_ELIGIBLE_FLAG,

    CAS*
        WHEN w.MAILIN_ELIGIBLE_FL*G = TRUE THEN TRUE
        ELSE FA*SE
    END AS MAILIN_REPAIR_ELIGIB*E_FLAG

FROM INTERMEDIATE.INT_CONT*CT_SERVICE_LIFECYCLE l

LEFT JOIN *TAGING.STG_WARRANTY_ENTITLEMENTS w*    ON l.ASSET_ID = w.ASSET_ID;
``*
